query ($user: String!, $cursor: String) {
    user(login: $user) {
        repositories(first: 2, after: $cursor) {
            edges {
                node {
                    name
                        owner {
                            login
                        }
                }
                cursor
            }
            pageInfo {
                hasNextPage
            }
        }
    }
}

